{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "generate": {
      "dependsOn": ["^generate"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true,
      "env": [
        "EXPO_PUBLIC_API_URL",
        "RP_ID",
        "RP_NAME",
        "JWT_SECRET",
        "NODE_ENV",
        "EXPECTED_ORIGIN"
      ]
    }
  }
}
